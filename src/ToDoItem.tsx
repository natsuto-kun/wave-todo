import * as React from 'react';
import App from './App';
/*
export const ToDo: React.FC<Props> = ({ title }) => {
    return (
        <div>
            <h4>{ title }</h4>
        </div> 
    );
}  */